# -*- Makefile -*-
#
# michael a.g. aïvázis <michael.aivazis@para-sim.com>
# (c) 1998-2026 all rights reserved


# the {conda-clang} image: a conda-forge clang toolchain and dependencies, distro-independent
pyre.conda-clang.name := conda-clang
pyre.conda-clang.root := etc/docker/

# {mm} and {pyre} both live one level above the pyre home; bind-mount that parent so the image
# builds and tests the live working tree; defer expansion so {pyre.home} is read at mount time
pyre.conda-clang.mounts.source = ${realpath $(pyre.home)/..}/
# mount both the build tool and the sources for {.run} and {.launch}
pyre.conda-clang.mounts := mm pyre


# end of file
