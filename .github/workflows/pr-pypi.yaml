# -*- coding: utf-8 -*-
#
# michael a.g. aïvázis <michael.aivazis@para-sim.com>
# (c) 1998-2026 all rights reserved

name: pr-pypi

# publish when a (published) GitHub Release is created, use the following:
on:
  pull_request:
  release:
    types:
      - published

# a new push to a pull request replaces the runs of the commits it supersedes, so they stop
# holding runners; runs for any other event are never cancelled
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build_wheels:
    name: build wheel on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-15]
        python: [cp311]

    steps:
      - uses: actions/checkout@v7
        with:
          # the whole history, so {setuptools_scm} can see the tag that names the version
          fetch-depth: 0

      - name: build wheels
        uses: pypa/cibuildwheel@v4.2.1
        env:
          CIBW_BUILD: ${{ matrix.python }}-*
          CIBW_SKIP: pp*

      - uses: actions/upload-artifact@v7
        with:
          # v4 rejects duplicate names across matrix cells
          name: pr-wheels-${{ matrix.os }}-${{ matrix.python }}
          path: ./wheelhouse/*.whl

  build_sdist:
    name: build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          # the whole history, so {setuptools_scm} can see the tag that names the version
          fetch-depth: 0

      - name: build sdist
        run: pipx run build --sdist

      - uses: actions/upload-artifact@v7
        with:
          name: pr-sdist
          path: dist/*.tar.gz


# end of file
