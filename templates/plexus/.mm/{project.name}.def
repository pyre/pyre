# -*- Makefile -*-
#
# {project.authors}
# {project.affiliations}
# (c) {project.span} all rights reserved
#

# global options for {{{project.name}}}
PROJECT = {project.name}

# default locations
PROJ_TMPDIR = $(BLD_TMPDIR)/$(PACKAGE)
PROJ_CLEAN += $(EXPORT_MODULEDIR)/$(PACKAGE)


# end of file 
