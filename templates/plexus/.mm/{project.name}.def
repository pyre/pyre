# -*- Makefile -*-
#
# {project.authors}
# {project.affiliations}
# (c) {project.span} all rights reserved
#

# global options for {{{project.name}}}
PROJECT = {project.name}
# the live host
PROJ_LIVE_HOST = {project.host.name}

# end of file
